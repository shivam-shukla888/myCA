{
  "name": "personal-ai-ca-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace proxy for Personal AI CA deployment",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "test": "cd backend && npm test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
